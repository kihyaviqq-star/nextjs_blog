# Блог на Next.js

Современный информационный портал о последних новостях и разработках в области искусственного интеллекта. Проект включает в себя профессиональный блог с админ-панелью, систему аутентификации и полностью адаптивный дизайн.

## 🚀 Tech Stack

- **Framework**: Next.js 15 (App Router, Turbopack)
- **Language**: TypeScript (strict mode)
- **Styling**: Tailwind CSS
- **UI Library**: shadcn/ui (Radix UI primitives)
- **Database**: Prisma ORM + SQLite (легко мигрировать на PostgreSQL)
- **Authentication**: NextAuth.js v5
- **Content Editor**: Editor.js с расширенными плагинами
- **Icons**: Lucide React
- **Animation**: Framer Motion

## ✨ Основные возможности

### 📰 Блог
- Адаптивная сетка статей с пагинацией (9 статей на страницу)
- Поиск по названию и содержимому в реальном времени
- Сортировка: Последние / Популярные
- Система тегов
- Счетчик просмотров
- Оценка времени чтения
- Эффект "spotlight" при наведении на карточки

### 👤 Профили пользователей
- Публичные страницы авторов (например, `/sara-chen`)
- Редактирование профиля (аватар, био, соц.сети)
- Автоматическая генерация username из email
- Защита зарезервированных имен

### 🔐 Система аутентификации
- NextAuth.js с Credentials провайдером
- Роли пользователей: USER, EDITOR, ADMIN
- Защищенные маршруты через middleware
- Хеширование паролей (bcryptjs)

### 🛠️ Админ-панель (для EDITOR и ADMIN)
- **Дашборд** (`/dashboard`): статистика (пользователи, посты, просмотры), топ статей
- **Управление статьями** (`/dashboard/articles`): список, создание, редактирование, удаление
- **Управление пользователями** (`/dashboard/users`): только для ADMIN
- **Rich Text Editor** (Editor.js): расширенные возможности форматирования

### 📱 Мобильная версия
- Полностью адаптивный дизайн
- Slide-out меню с использованием shadcn/ui Sheet
- Touch-friendly элементы (минимум 44px высота)
- Оптимизированные отступы и типография

### 🎨 Дизайн
- Dark mode first (переключатель светлой/темной темы)
- Эффект размытия на header (`backdrop-blur`)
- Hover-эффекты и плавные переходы
- Консистентная цветовая схема

## 🛠️ Локальная установка

### Требования
- Node.js 18+ 
- npm или yarn

### Шаги

```bash
# 1. Клонировать репозиторий
git clone https://github.com/kihyaviqq-star/nextjs_blog
cd nextjs_blog

# 2. Установить зависимости
npm install

# 3. Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env:
# - DATABASE_URL="file:./dev.db"
# - NEXTAUTH_URL="http://localhost:3000"
# - NEXTAUTH_SECRET="<случайная строка>"

# 4. Инициализировать базу данных
npx prisma generate
npx prisma migrate dev

# 5. Заполнить базу тестовыми данными (опционально)
npx prisma db seed

# 6. Запустить dev сервер
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Тестовые аккаунты (после seed)

- **Редактор**: `editor@ai-stat.ru` / `editor123`
- **Пользователь**: `user@ai-stat.ru` / `user123`

## 📁 Структура проекта

```
├── app/                      # Next.js App Router
│   ├── (auth)/              # Группа маршрутов для аутентификации
│   ├── [username]/          # Динамический роут для профилей
│   ├── blog/[slug]/         # Страница статьи
│   ├── dashboard/           # Админ панель
│   ├── settings/            # Настройки пользователя
│   └── api/                 # API Routes
├── components/
│   ├── blog/                # Компоненты блога
│   ├── editor/              # Editor.js обертка
│   └── ui/                  # shadcn/ui компоненты
├── lib/
│   ├── auth.ts              # NextAuth конфигурация
│   ├── prisma.ts            # Prisma Client
│   └── utils.ts             # Утилиты
├── prisma/
│   ├── schema.prisma        # Database схема
│   └── seed.ts              # Seed скрипт
└── scripts/                 # Утилиты для управления БД
```

## 🚀 Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Настройте переменные окружения:
   - `DATABASE_URL` (Postgres на Vercel/Railway/Supabase)
   - `NEXTAUTH_URL` (URL вашего приложения)
   - `NEXTAUTH_SECRET`
3. Deploy!

```bash
vercel
```

### Другие платформы

Проект совместим с любой платформой, поддерживающей Next.js 15.

## 🔧 Основные команды

```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка для production
npm start            # Запуск production сборки
npm run lint         # Линтинг кода

npx prisma studio    # GUI для базы данных
npx prisma generate  # Генерация Prisma Client
npx prisma db push   # Синхронизация схемы с БД
```

## 📄 Лицензия

MIT

## 🤝 Вклад

Pull requests приветствуются! Для больших изменений, пожалуйста, сначала откройте issue для обсуждения.

---

**Создано с ❤️ для всех, кто интересуется ИИ**
