-- RedefineTables
PRAGMA defer_foreign_keys=ON;
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Software" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "slug" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "shortDesc" TEXT NOT NULL,
    "websiteUrl" TEXT,
    "logoUrl" TEXT,
    "pricing" TEXT NOT NULL DEFAULT 'Free',
    "platforms" TEXT,
    "licenseType" TEXT,
    "isAutoGenerated" BOOLEAN NOT NULL DEFAULT false,
    "sourceUrl" TEXT,
    "lastCrawledAt" DATETIME,
    "categoryId" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    CONSTRAINT "Software_categoryId_fkey" FOREIGN KEY ("categoryId") REFERENCES "SoftwareCategory" ("id") ON DELETE RESTRICT ON UPDATE CASCADE
);
INSERT INTO "new_Software" ("categoryId", "createdAt", "description", "id", "logoUrl", "name", "pricing", "shortDesc", "slug", "updatedAt", "websiteUrl") SELECT "categoryId", "createdAt", "description", "id", "logoUrl", "name", "pricing", "shortDesc", "slug", "updatedAt", "websiteUrl" FROM "Software";
DROP TABLE "Software";
ALTER TABLE "new_Software" RENAME TO "Software";
CREATE UNIQUE INDEX "Software_slug_key" ON "Software"("slug");
CREATE INDEX "Software_categoryId_idx" ON "Software"("categoryId");
CREATE TABLE "new_SoftwareCategory" (
    "id" TEXT NOT NULL PRIMARY KEY,
    "slug" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "icon" TEXT,
    "parentId" TEXT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT "SoftwareCategory_parentId_fkey" FOREIGN KEY ("parentId") REFERENCES "SoftwareCategory" ("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_SoftwareCategory" ("createdAt", "icon", "id", "name", "slug") SELECT "createdAt", "icon", "id", "name", "slug" FROM "SoftwareCategory";
DROP TABLE "SoftwareCategory";
ALTER TABLE "new_SoftwareCategory" RENAME TO "SoftwareCategory";
CREATE UNIQUE INDEX "SoftwareCategory_slug_key" ON "SoftwareCategory"("slug");
PRAGMA foreign_keys=ON;
PRAGMA defer_foreign_keys=OFF;
