#!/bin/bash
# Скрипт для исправления прав доступа к загруженным файлам

echo "🔧 Исправление прав доступа к загруженным файлам..."

# Путь к проекту
PROJECT_DIR="/var/www/mlcrosoft.ru"

# Устанавливаем права на директории (755 - read/execute для всех, write только для владельца)
find "$PROJECT_DIR/public/uploads" -type d -exec chmod 755 {} \;

# Устанавливаем права на файлы (644 - read для всех, write только для владельца)
find "$PROJECT_DIR/public/uploads" -type f -exec chmod 644 {} \;

# Устанавливаем владельца (замените www-data на вашего пользователя веб-сервера)
chown -R www-data:www-data "$PROJECT_DIR/public/uploads"

echo "✅ Права доступа исправлены"
echo ""
echo "Проверка:"
ls -la "$PROJECT_DIR/public/uploads/"
