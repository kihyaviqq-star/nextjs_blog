# 🚀 Root Level Usernames - Quick Guide

## ✅ Что реализовано

### 1. Профили на корневом уровне
```
localhost:3000/nikita    → Профиль пользователя Nikita
localhost:3000/settings  → Системная страница (приоритет)
localhost:3000/admin     → Системная страница (приоритет)
```

### 2. Защита от конфликтов
- ✅ 100+ зарезервированных имен
- ✅ Проверка при сохранении профиля
- ✅ 404 для несуществующих пользователей

### 3. Валидация username
- **Длина:** 3-30 символов
- **Формат:** `a-z`, `A-Z`, `0-9`, `-`, `_`
- **Начинается с:** Буквы или цифры
- **Уникальность:** Проверка в БД

## 📁 Созданные файлы

### `lib/constants.ts`
Список зарезервированных имен и функции валидации:
- `RESERVED_USERNAMES` - массив зарезервированных имен
- `isUsernameReserved()` - проверка резервации
- `validateUsername()` - полная валидация
- `generateUsernameSlug()` - генерация slug

### `app/[username]/page.tsx`
Страница профиля пользователя:
- Динамический маршрут на корневом уровне
- Загрузка данных из Prisma
- 404 для зарезервированных/несуществующих

### Обновлен: `app/api/profile/route.ts`
API проверяет username при сохранении:
- Валидация формата
- Проверка резервации
- Проверка уникальности

## 🧪 Как протестировать

### 1. Перезапустите сервер
```bash
# Ctrl+C для остановки
npm run dev
```

### 2. Тест: Системный маршрут
```
URL: http://localhost:3000/settings

Ожидается:
✅ Открывается страница настроек
❌ НЕ профиль пользователя "settings"
```

### 3. Тест: Профиль пользователя
```bash
# Найдите имя пользователя в БД
npx prisma studio

# Откройте профиль (например, имя "Сара Чен")
URL: http://localhost:3000/sara-chen
# или
URL: http://localhost:3000/Сара-Чен

Ожидается:
✅ Профиль пользователя
✅ Аватар, био, соцсети
✅ "Участник с ..."
```

### 4. Тест: Несуществующий пользователь
```
URL: http://localhost:3000/doesnotexist

Ожидается:
❌ 404 Not Found
```

### 5. Тест: Зарезервированное имя (профиль)
```
Попробуйте открыть:
- http://localhost:3000/admin
- http://localhost:3000/api
- http://localhost:3000/blog

Ожидается:
✅ Системные страницы (НЕ профили)
```

### 6. Тест: Изменение username (Settings)
```
1. Войдите: editor@ai-stat.ru / editor123
2. Откройте: /settings
3. Измените имя на "login"
4. Нажмите "Сохранить"

Ожидается:
❌ Toast error: "Этот никнейм зарезервирован системой"
```

```
1. Измените имя на "MyUsername"
2. Нажмите "Сохранить"

Ожидается:
✅ Toast success: "Профиль успешно обновлен!"
✅ Профиль доступен: /myusername
```

## 🎯 Зарезервированные имена

### Системные маршруты
```
admin, settings, login, signin, signup, register,
logout, auth, dashboard, profile, account
```

### API и технические
```
api, _next, static, public, assets, uploads
```

### Контент
```
blog, post, posts, article, articles, author,
authors, editor, write, create, edit, delete
```

### Статические файлы
```
favicon, robots, sitemap, manifest
```

### Расширения файлов
```
js, css, json, xml, html, txt, ico, png, jpg
```

**Полный список:** См. `lib/constants.ts`

## 📝 Примеры валидации

### ✅ Валидные username

```typescript
"nikita"         → ✅ OK
"john-doe"       → ✅ OK
"user_123"       → ✅ OK
"AlexSmith"      → ✅ OK (преобразуется в alexsmith)
"developer99"    → ✅ OK
```

### ❌ Невалидные username

```typescript
"ab"             → ❌ Слишком короткий (min 3)
"a"              → ❌ Слишком короткий
"user@domain"    → ❌ Спецсимволы запрещены
"-username"      → ❌ Нельзя начинать с дефиса
"_username"      → ❌ Нельзя начинать с подчеркивания
"admin"          → ❌ Зарезервировано
"login"          → ❌ Зарезервировано
"favicon.ico"    → ❌ Расширение файла
```

## 🔧 API Usage

### Проверка валидности
```typescript
import { validateUsername } from "@/lib/constants";

const result = validateUsername("nikita");

if (result.valid) {
  console.log("✅ Username valid");
} else {
  console.error("❌", result.error);
}
```

### Генерация slug
```typescript
import { generateUsernameSlug } from "@/lib/constants";

const slug = generateUsernameSlug("John Doe");
// Result: "john-doe"

const slug2 = generateUsernameSlug("Сара Чен");
// Result: "sara-chen" (if Cyrillic, or "")
```

## 🛠️ Troubleshooting

### Проблема: 404 на моем username

**Решение:**
1. Проверьте БД: `npx prisma studio`
2. Убедитесь, что пользователь существует
3. Проверьте написание (регистр не важен)

### Проблема: Не могу сохранить желаемое имя

**Причины:**
1. Зарезервировано → Попробуйте вариант: `nikita-dev`
2. Занято → Кто-то уже использует
3. Невалидный формат → Проверьте правила

### Проблема: Системный маршрут показывает профиль

**Решение:**
1. Очистите кеш: `rm -rf .next`
2. Перезапустите сервер
3. Проверьте `app/settings/page.tsx` существует

## 📚 Полная документация

См. `ROOT-LEVEL-USERNAMES.md` для:
- Детальная архитектура
- Безопасность
- Будущие улучшения
- API endpoints
- Миграция

## ✨ Результат

**Теперь у вас:**
- ✅ Чистые SEO-friendly URL (`/username`)
- ✅ Защита системных маршрутов
- ✅ Валидация и проверка уникальности
- ✅ Интеграция с Prisma
- ✅ 404 для несуществующих
- ✅ Нет конфликтов со статикой

**Готово к тестированию!** 🚀

---

**Следующий шаг:** Перезапустите сервер и протестируйте все сценарии выше!
